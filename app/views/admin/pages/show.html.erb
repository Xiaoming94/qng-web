<p id="notice"><%= notice %></p>
<div class="admin-container">
	<div class="card white black-text">
		<div class="card-content">
			<strong>Title:</strong>
			<h3 class="preview-title"><%= @page.title %></h3>

			<div class="preview-content">
				<p>
					<strong>Body:</strong>
					<%= markdown @page.body %>
				</p>
			</div>
		</div>
	</div>
</div>	
<%= link_to 'Edit', edit_admin_page_path(@page), class: "btn waves-effect waves-light blue black-text" %>
<%= link_to 'Back', admin_pages_path, class: "btn blue waves-effect waves-light black-text" %>
